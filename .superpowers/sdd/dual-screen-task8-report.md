# Dual-screen Task 8 report

- State: Studio now turns the six authenticated Helper projection jobs into one concise native setup action: Detect → Open → Assign → Fullscreen, followed by the attended witness action. This phase does not claim physical certification because no visible Win11 dual-screen witness was completed here.
- Trust boundary: strict schemas and a deterministic reducer correlate command, session, generation, course version, slide deck, runtime manifest, and digest. Extra fields, stale receipts, mismatched identities, rejected operations, and browser rehearsal cannot produce certification.
- Window policy: the default is external Stage plus internal-primary Presenter. Swap is explicit and immediately invalidates any prior witness. Published projection identity changes remount the native controller, invalidate local state, and best-effort close the prior session.
- Recovery boundary: Retry first confirms the prior native session is closed and stops if close is rejected. Escape or Close during an in-flight command queues a visible safe-close request; success, timeout, failure, unmount, and identity-change paths all attempt bounded cleanup before any replacement flow.
- Interface: the light-theme setup uses four compact steps and accessible 44px controls for Swap, Retry, Re-witness, Close, browser rehearsal, and return to edit. Internal IDs, UUIDs, digests, display IDs, HWNDs, paths, codes, and process details are never rendered.
- Verification: focused projection/controller tests passed 30 tests; the complete Web suite passed 305 tests; TypeScript typecheck, production build, `git diff --check`, and focused platform QA passed. Platform QA included 167 Python tests and every product gate; only the known inaccessible root `.pytest_cache` warnings remained.
- Review learning: the bounded read-only review found five Important lifecycle defects across two passes: identity drift retained certification, fallback/edit could leave native windows open, busy Close could be ignored, unmount failure could skip cleanup, and Retry could proceed after rejected close. All were closed with regression tests. Final review reported no Critical, Important, or Minor findings.
- Routing: this UI/state-machine phase remained P1 with a `gpt-5.6-terra` medium recommendation. No model-switch receipt was available, and Ultra was not needed.
- Artifact lifecycle: only regenerated Web/QA caches were removed. Source, tests, evidence, dependencies, manifests, databases, and valid Git history are retained.
- Next phase: Task 9 publishes and installs the Host, runs the explicit integration gate, and then requires an attended visible Win11 extended-desktop witness before personal-device certification can be recorded.
